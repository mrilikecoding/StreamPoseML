{
  "name": "mock_bluetooth",
  "version": "1.0.0",
  "description": "mock bluetooth service for StreamPoseML",
  "main": "mock_bluetooth.js",
  "scripts": {
    "start": "node mock_bluetooth.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bleno": "^0.5.0"
  }
}
